Ejercicio de JavaScript, cuyo objeto es el desarrollo de una función que determine si una cadena contiene la palabra "English".

Algoritmo:

Recibir un texto.
Convertir el texto en minúsculas.
Buscar si el texto contiene la palabra "english".
Si la contiene, devolver "true".
Si no la contiene, devolver "false".
